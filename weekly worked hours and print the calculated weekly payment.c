#include<stdio.h>
#include<conio.h>
void main()
{
  int hour,payment;
  printf("Enter the number of hour worked on this week");
  scanf("%d",&d);
  if (x<42)
  {
    y=x*15;
  printf("Your payment is:Rs%d",y)
    }
  else
  {
    y=42*15+(x-42)*20;
  printf("your pay for this week is:Rs%d",y);
  }
  getch();
      
}
